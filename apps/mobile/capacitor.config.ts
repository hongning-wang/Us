import type {CapacitorConfig} from '@capacitor/cli';
const config: CapacitorConfig = {
 appId:'app.us.mobile', appName:'Us', webDir:'dist',
 ios:{contentInset:'never',backgroundColor:'#ffffff'},
};
export default config;
